
% DAVOR: das referenzen angezeigt werden:
% ========================================
% latex word.tex
% bibtex word 
% latex word.tex


% WICHTIG: windows cmd.exe shell benutzen
% ========================================
% PATH=C:\Programme\cygwin\bin;%PATH%
% htlatex word.tex "html,word" "symbol/!" "-cvalidate"

% einfach <enter> um 3 kleine fehler zu ignorieren (``read'', ``take'')

% Ollies Anleitung in Word befolgen für HTML --> WORD conversion:
% http://wiki.flupp.de/latex/tex4ht

% Hier die Kurzform:
% - Das .html in word öffnen
% - Word 2007: Schaltfläche Office / Vorbereiten / Verknüpfungen mit Datei bearbeiten / 
%   dort alle aufgeführten Verknüfungen aufheben
% - Speichern unter..
% - Extras / Speicheroptionen / Speichern / Truetype Schriftarten einbetten / OK
% - Die Stylesheetfehlermeldung ignorieren.
% - Speichern


\documentclass{book}

\usepackage[american]{babel}
\usepackage[ansinew]{inputenc}

% shared/template.tex
\usepackage{color,calc,graphicx,soul}
\usepackage[dvipsnames, table]{xcolor}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{xspace}
\usepackage{url}
\usepackage[center]{subfigure}
\usepackage[chapter]{algorithm}
\usepackage{algpseudocode}
\usepackage{algorithmicx}
\usepackage{tabularx}
\usepackage[T1]{fontenc} % T1 Schrift Encoding



\newcommand*\Descriptionlabel[1]
{\raisebox{0pt}[1ex][0pt]
  {\makebox[\labelwidth][1]
    {\parbox[t]{\labelwidth}
      {\hspace{0pt}\textbf{#1:}}}}}

\newcommand*\Descriptionlabelx[1]
{\parbox[t]{\textwidth}
  {\textbf{#1}\\\mbox{}}
}

\newenvironment{Description}
{\begin{list}{}{\let\makelabel\Descriptionlabelx
    \setlength\labelwidth{1em}
    \setlength\leftmargin{\labelwidth+\labelsep}}}
{\end{list}}



% commands.tex
%Anstatt $|x|$ $\abs{x}$ verwenden. Die Betragsstriche skalieren automatisch, falls "x" etwas groessers sein sollte...
\newcommand{\abs}[1]{\left\lvert#1\right\rvert}
%
%
\newcommand{\commentchar}{\ensuremath{/\mkern-4mu/}}

% credit goes to olly!
\newcommand{\el}[1]{\mathtt{#1}}
\newcommand{\set}[1]{\ensuremath{\mathcal{#1}}}	%sets in definition
\newcommand{\fn}[1]{\ensuremath{\mathsf{#1}}}
\newcommand{\scfn}[1]{\textsc{#1}}
\newcommand{\xmlcode}[1]{\texttt{#1}}	%reference to BPEL XML code
\newcommand{\true}{\ensuremath{\el{true}}\xspace}
\newcommand{\false}{\ensuremath{\el{false}}\xspace}

% additional defs for amsmath (http://www.risc.uni-linz.ac.at/people/hemmecke/AldorCombinat/combinatsu6.html)
\newcommand{\PowerSet}[1]{\ensuremath{\protect\mathcal{P}(#1)}}
\newcommand{\setFont}{\protect\mathbb}
\newcommand{\fieldFont}{\protect\mathbb}
\newcommand{\setC}{\setFont{C}} % complex numbers
\newcommand{\setN}{\setFont{N}} % natural numbers
\newcommand{\setQ}{\setFont{Q}} % rational numbers
\newcommand{\setR}{\setFont{R}} % real numbers
\newcommand{\setZ}{\setFont{Z}} % integers
\newcommand{\setB}{\setFont{B}} % booleans  
\newcommand{\Set}[1]{\left\{#1\right\}}
\newcommand{\setDef}[2]{{#1}\left|\,\vphantom{#1}{#2}\right.}
\newcommand{\SetDef}[2]{\Set{\,\setDef{#1}{#2}\,}}
\newcommand{\card}[1]{\left|#1\right|}
\newcommand{\bigO}{\ensuremath{\mathcal{O}}}


% theorem environments here..
\newtheorem{definition}{Definition} %definition
\newtheorem{theorem}{Theorem} %satz
\newtheorem{proposition}{Proposition} %behauptung
\newtheorem{proof}{Proof} %beweis

% Daniels custom commands
% -----------------------
\newcommand{\todo}[1]{\hl{TODO: #1}}
\newcommand{\CF}{\texttt{CF}\xspace}
\newcommand{\DP}{\texttt{DP}\xspace}
\newcommand{\FAIL}{\texttt{FAILED}\xspace}
\newcommand{\FAILED}{\texttt{FAILED}\xspace}
\newcommand{\FAULT}{\texttt{FAILED}\xspace}
\newcommand{\DATA}{\texttt{DATA}\xspace}

\newcommand{\eg}{\textit{e.g.}\xspace} % e.g. 
\newcommand{\ie}{\textit{i.e.}\xspace} % i.e. 
\newcommand{\cf}{\textit{cf.}\xspace} % i.e. 

% TODO: note that this is a different def than used in the original 
% --> htlatex doesnt like our original definition of \tuple
\newcommand{\tuple}[1]{(#1)}



\setcounter{topnumber}{1} % max floats at top of a page
\setcounter{bottomnumber}{1} % max floats at bottom of a page
\setcounter{totalnumber}{2} % max total floats per page

\renewcommand{\textfraction}{0.05} % pages with 5% text are still okay
\renewcommand{\topfraction}{0.95} % a float may occupy 95% of a page and still moved to top
\renewcommand{\bottomfraction}{0.95} % a float may occupy 95% of a page and still moved to bottom
\renewcommand{\floatpagefraction}{0.35} % may start a new page when 35% of a a floatpage 
										%(page that only consists of floats) is occupied




\begin{document}

% von olly --> macht blocksatz im HTML
% ACHTUNG: tut nur mit tex4ht.exe, NICHT mit latex.exe
%\Css{body {text-align:both}}

% we do not produce a PDF..
%\pdfoutput=0


\input{abstract}
\input{introduction}

\input{chapter2}
\input{chapter3}
\input{chapter4}
\input{chapter5}
\input{chapter6}
\input{chapter7}

\input{conclusion}


\bibliographystyle{plain}
\bibliography{bibliography}

\end{document}